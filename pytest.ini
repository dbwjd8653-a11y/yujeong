[pytest]
pythonpath = .
testpaths = tests performance

addopts = -v -s --tb=short --alluredir=allure-results --clean-alluredir --continue-on-collection-errors
markers =
    slow: 실행 시간이 긴 테스트 (AI 생성, 다운로드 등)
    xfail: 알려진 버그로 인해 실패가 예상되는 테스트
